<% form_tag "/events/#{@event.id}/notes" do %>
  <%= text_area_tag :notes, placeholder: 'Notes for admins', class: 'autosize form-control', onchange: '$(this.form).submit()', value: @event.notes %>
<% end %>
