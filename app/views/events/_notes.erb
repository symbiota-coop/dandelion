<% form_tag "/events/#{@event.id}/notes" do %>
  <%= text_area_tag :notes, placeholder: 'Private notes (visible to event admins only)', class: 'autosize form-control', onchange: '$(this.form).submit()', value: @event.notes %>
<% end %>
