<% form_tag "/events/#{@event.id}/notes" do %>
  <%= text_area_tag :notes, placeholder: 'Private notes (visible to event admins only)', class: 'autosize form-control shadow-sm', onchange: '$(this.form).submit()', value: @event.notes %>
<% end %>
