<% if (badge = EventPaymentMethod.badge_for(record)) %>
  <span class="label label-default <%= badge[:class] %> mr-1"<% if badge[:style] %> style="<%= badge[:style] %>"<% end %>><%= badge[:text] %></span>
<% end %>
