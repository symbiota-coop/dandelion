<div class="main">
  <p>Hi %recipient.firstname%,</p>
  <p>
    We received an error from Stripe when <%= account.name %> (<%=account.email%>) attempted to purchase ticket(s) for the event <a href="<%= ENV['BASE_URI'] %>/e/<%=event.slug%>"><%= event.name %></a>:
  </p>
  <blockquote>
    <%= error.message %>
  </blockquote>
  <p>
    We recommend you log into your Stripe account and resolve any warnings or notifications you see there.
  </p>
</div>
