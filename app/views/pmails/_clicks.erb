<% if @pmail.pmail_links.exists? %>
  <table class="table">
    <thead>
      <tr>
        <th style="width: 1px">Link</th>
        <th>Clicks</th>
      </tr>
    </thead>
    <tbody>
      <% max_clicks = 0; @pmail.pmail_links.order('clicks desc').limit(20).each_with_index { |link,i| max_clicks = link.clicks if i == 0; w = (link.clicks.to_f/max_clicks)*100; event = link.event; safe_url = link.url.to_s.match?(%r{\Ahttps?://}i) %>
      <tr>
        <td>
          <% if safe_url %>
            <a class="<%= 'text-dark' unless event %>" target="_blank" rel="noopener noreferrer" href="<%= link.url %>"><%= event ? link.event.name : link.url %></a>
          <% else %>
            <span class="<%= 'text-dark' unless event %>"><%= event ? link.event.name : link.url %></span>
          <% end %>
        </td>
        <td>
          <div class="progress">
            <div class="progress-bar" role="progressbar" style="width: <%= "#{w}%" %>;"><%= link.clicks %></div>
          </div>
        </td>
      </tr>
      <% } %>
    </tbody>
  </table>
<% end %>